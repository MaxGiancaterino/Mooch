type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
}

type Group {
  id: ID! @unique
  name: String!
  members: [User!]!
}

type Payment {
  id: ID! @unique
  name: String!
  cost: Float!
}
